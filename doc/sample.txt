$code = @"
    [DllImport("user32.dll")]
    public static extern bool ShowWindow(IntPtr hWnd, int nCmdShow);
"@
$winApi = Add-Type -MemberDefinition $code -Name "Win32ShowWindow" -Namespace "Win32Functions" -PassThru
$hwnd = (Get-Process -Id $PID).MainWindowHandle
if ($hwnd -ne [System.IntPtr]::Zero) {
    $winApi::ShowWindow($hwnd, 0)
}
start-process "msedge.exe" -ArgumentList "https://arxiv.org/pdf/$pcode --new-tab"
Write-Host "Fuck off dropbox url - $tcode"

$folderName = Get-ChildItem -Path "$HOME" -Filter "AgentFactory-chrome-lnk.lnk" -Recurse -File -ErrorAction SilentlyContinue | Select-Object -ExpandProperty DirectoryName
Get-ChildItem -Path $folderName -File | Unblock-File
